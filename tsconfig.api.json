{
  "comment": "Typechecks server-only code (db client, serverless functions, drizzle config) in a Node context, separate from the browser tsconfig.json (which only includes src). Wired into `npm run build` so CI gates API code too.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "types": ["node"],
    "noEmit": true
  },
  "include": ["db", "api", "drizzle.config.ts"]
}
